module.exports = {
    content: [
        './**/*.html',  // Adjust this to match the paths to your HTML files
        './**/*.js',    // Adjust this to match the paths to your JS files
        './**/*.cshtml' // Adjust this if you're using Razor Views
    ],
    css: ['./path-to-your-css/*.css'], // Adjust this to point to your CSS files
    output: './path-to-output-folder',  // Adjust this to the folder where the minified CSS should be saved
}
